/**
 * The Knowledge Arena's public surface: the screen itself, and the summary the
 * profile folds in through `useGameCareer`.
 */
export { SportQuiz } from "./components/sport-quiz";

export { useQuizStats, type QuizStats } from "./state/quiz-progress";
