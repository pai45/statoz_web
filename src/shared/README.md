# Shared

Reusable, non-visual building blocks such as hooks, providers, configuration,
and utilities live here. Visual tokens and reusable interface components
belong in `src/design-system`.

Shared modules must not import from `src/app` or `src/features`.
