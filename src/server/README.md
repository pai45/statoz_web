# Server

This directory contains server-only code: external service integrations,
repositories, persistence, and orchestration. Do not import these modules into
Client Components. Expose browser-required operations through Server
Components, Server Actions, or route handlers as appropriate.
