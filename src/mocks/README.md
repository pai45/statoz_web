# Demo fixture modules

`src/mocks` is the source of declarative data used to render the current demo.
Import a focused module (for example `@/mocks/matches`) rather than a root
barrel. Feature modules retain selectors, state, storage, and other behaviour.

Generated asset registries, gameplay engines, and design/configuration values
remain with their owners and are intentionally not duplicated here.
